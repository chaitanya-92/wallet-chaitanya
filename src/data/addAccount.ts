import type { AddAccountDialogContent } from "@/types"

export const addAccountContent: AddAccountDialogContent = {
  title: "Create New Account",
  placeholder: "Enter account name",
  cancelButton: "Cancel",
  createButton: "Create Account",
}
